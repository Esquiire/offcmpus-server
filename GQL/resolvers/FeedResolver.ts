import {Resolver} from 'type-graphql'

/**
 * FeedResolver =>
 * @desc The feed resolver handles queries pertainint to
 * retrieving feed data for a student.
 */

@Resolver()
export class FeedResolver {

}